name: bundle-python-embed-venv

on:
  push:
    tags:
    - 'v*'

jobs:
  build-distro:
    name: Bundle Windows Embeddable Python with pip and virtualenv
    runs-on: windows-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v2
      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: '3.9'
      - name: Run Bundle script
        run: .\Bundle-Python.ps1 -PythonVersion "3.9.1"
        shell: pwsh
      - name: Create Release
        id: create_release
        uses: actions/create-release@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          tag_name: ${{ github.ref }}
          release_name: Release ${{ github.ref }}
          draft: false
          prerelease: false
      - name: Upload Release Asset
        id: upload-release-asset
        uses: actions/upload-release-asset@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          upload_url: ${{ steps.create_release.outputs.upload_url }} # This pulls from the CREATE RELEASE step above, referencing it's ID to get its outputs object, which include a `upload_url`. See this blog post for more info: https://jasonet.co/posts/new-features-of-github-actions/#passing-data-to-future-steps 
          asset_path: ./python.zip
          asset_name: python-3.9.1-embed-amd64-venv.zip
          asset_content_type: application/zip
#      - name: Archive artifact
#        uses: actions/upload-artifact@v2
#        with:
#          name: python-3.9.1-embed-amd64-venv
#          path: python/
